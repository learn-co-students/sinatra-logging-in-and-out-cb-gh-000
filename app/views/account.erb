<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">

    <% if Helpers.is_logged_in?(session) %>
      <a href="/logout">logout</a>
      <h1>Welcome <%= Helpers.current_user(session).name %></h1>

      <h3>Your Balance: <%= Helpers.current_user(session).balance %></h3>

    <% else %>
      <a href="/index">index</a>
      <% end %>

  </body>
</html>
